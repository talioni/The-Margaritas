# Keep the build context small: ship only the one model file we need, not the
# whole training history / datasets.
runs/
!runs/classify/
!runs/classify/train_v2-6/
!runs/classify/train_v2-6/weights/
!runs/classify/train_v2-6/weights/best.pt

dataset*/
.venv/
venv/
.git/
.gitignore
__pycache__/
**/__pycache__/
*.pyc
*.fzz
schematic/
*.md
